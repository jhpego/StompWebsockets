/// <reference types="node" />
import { StompEventEmitter } from './model';
import { WebSocket } from './utils';
import { Socket } from 'net';
export declare type StompStreamEvent = 'data' | 'end';
export interface StompStreamLayer {
    emitter: StompEventEmitter<StompStreamEvent>;
    send(data: string): Promise<void>;
    close(): Promise<void>;
}
export declare function openStream(socket: Socket | WebSocket): StompStreamLayer;
